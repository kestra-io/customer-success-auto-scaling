{
  "name": "kestra-autoscaling-trigger-app",
  "version": "1.0.0",
  "private": true,
  "description": "Slider-driven webhook trigger + live pending/capacity readout for the Kestra auto-scaling example",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
